# Analysis: 서재-강화

**Feature:** 서재-강화  
**분석일:** 2026-05-19  
**Phase:** Check  
**Match Rate:** 95%

---

## Context Anchor

| 항목 | 내용 |
|------|------|
| **WHY** | 소설 수가 늘수록 제목 텍스트만으로는 시각 인식 속도가 느려짐 |
| **WHO** | 글방 사용자 — 소설 5개 이상 보유하거나 자주 새 소설을 추가하는 독자 |
| **RISK** | 낮음 — NovelCard/LibraryPage만 수정, 신규 컴포넌트 파일 없음 |
| **SUCCESS** | 커버 블록 렌더링 + 정렬 즉시 반영 + 로딩 스켈레톤 표시 |
| **SCOPE** | NovelCard.jsx, LibraryPage.jsx, useNovels.js 3개 파일 수정 |

---

## 1. 분석 방법

- 설계 문서 vs 구현 코드 정적 비교 (3개 축: Structural / Functional / Contract)
- 런타임 서버 없음 → Static-only 분석
- API 변경 없음 → Contract 검사 N/A

---

## 2. Structural Match — 100%

| 항목 | 설계 | 구현 | 상태 |
|------|------|------|------|
| 수정 파일 수 | 3개 | 3개 | ✅ |
| 신규 파일 | 0개 | 0개 | ✅ |
| accentBar 제거 | 완전 제거 | Grep 결과 0건 확인 | ✅ |
| cover CSS 클래스 | 추가 | NovelCard.jsx L24 | ✅ |
| contentWrap CSS 클래스 | 추가 | NovelCard.jsx L37 | ✅ |
| SORT_KEY 상수 | `'geulbang_sort'` | LibraryPage.jsx L13 | ✅ |
| SORT_OPTIONS 상수 | 3개 선택지 | LibraryPage.jsx L14 | ✅ |
| skeletonCard CSS 클래스 | 추가 | LibraryPage.jsx L96 | ✅ |
| loading 상태 | useNovels 반환 | useNovels.js L48 | ✅ |

---

## 3. Functional Depth — 100%

| ID | 요구사항 | 코드 위치 | 상태 |
|----|---------|----------|------|
| SF-01 | 커버 블록 64×80px | NovelCard.jsx L25-26 `width:'64px';height:'80px'` | ✅ |
| SF-02 | 이니셜 1자 + titleToHsl 배경 | L118-119 `charAt(0)` + `style={{ background: accentColor }}` | ✅ |
| SF-03 | 4px 사이드바 → 커버 블록 대체 | accentBar 0건 (Grep 확인), cover div 추가 | ✅ |
| SF-04 | 정렬 드롭다운 소설 목록 상단 | LibraryPage.jsx L307 `<select className={sortSelect}` | ✅ |
| SF-05 | 3가지 정렬 선택지 | SORT_OPTIONS: lastRead/title/progress | ✅ |
| SF-06 | localStorage 저장/복원 | L208 `getItem(SORT_KEY)`, L219 `setItem(SORT_KEY, value)` | ✅ |
| SF-07 | loading 상태 → 스켈레톤 3개 | useNovels `loading` + L258-277 `[0,1,2].map(...)` | ✅ |
| — | 소설 0개 시 드롭다운 미표시 | sortSelect가 `novels.length > 0` 분기에만 존재 | ✅ |
| — | 정렬 클라이언트 사이드 | useMemo + `[...novels].sort()` — Firestore 쿼리 변경 없음 | ✅ |
| — | 스켈레톤 별도 파일 없음 | LibraryPage.jsx 인라인 JSX | ✅ |
| — | user 변경 시 loading 재설정 | useNovels.js useEffect: `setLoading(true)` on user change | ✅ |

---

## 4. API Contract — N/A

순수 프론트엔드 변경. Firebase 호출 구조 변경 없음. API 계약 검사 미적용.

---

## 5. Success Criteria 최종 상태

| ID | 기준 | 상태 | 근거 |
|----|------|------|------|
| SC-01 | NovelCard 커버 블록(이니셜 + 배경색) 표시 | ✅ | `cover` div + `charAt(0)` + `accentColor` |
| SC-02 | 정렬 드롭다운 소설 목록 상단 표시 (소설 ≥ 1) | ✅ | `novels.length > 0` 분기 안에만 sortSelect 존재 |
| SC-03 | 정렬 선택 시 목록 즉시 재정렬 | ✅ | `sortedNovels` useMemo 의존성 `[novels, sortOrder]` |
| SC-04 | 새로고침 후 정렬 선택값 유지 | ✅ | `useState(() => localStorage.getItem(SORT_KEY) \|\| 'lastRead')` |
| SC-05 | 로딩 중 스켈레톤 카드 3개 표시 | ✅ | `loading` true 분기 → `[0,1,2].map(...)` |
| SC-06 | 온보딩(소설 0개)에서 정렬 드롭다운 미표시 | ✅ | 3단 조건 분기: loading / empty / list 분리 |

**6/6 충족** ✅

---

## 6. Match Rate

```
Structural: 100%
Functional: 100%
Contract:   N/A

Overall (Static) = (100% × 0.35) + (100% × 0.65) = 100%
런타임 미검증 보정 → 95%
```

**최종 Match Rate: 95%** ✅ (90% 기준 통과)

---

## 7. 잔여 검증 항목 (런타임)

브라우저에서 직접 확인이 필요한 항목.

- [ ] 커버 블록에 한국어 첫 글자 정상 표시 (예: '재', '귀', '전')
- [ ] 정렬 드롭다운 변경 시 목록 즉시 재정렬 육안 확인
- [ ] 새로고침 후 정렬값 유지 확인 (localStorage 탭에서 `geulbang_sort` 키 확인)
- [ ] 로딩 스켈레톤 표시 확인 (네트워크 쓰로틀링으로 시뮬레이션)
- [ ] 소설이 없는 계정 접속 시 드롭다운 미표시 확인
